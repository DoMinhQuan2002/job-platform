# Job Platform Admin

Next.js admin app. Same stack as `apps/frontend` (Tailwind + shadcn), isolated so admin UI is not shipped with the public site.

```bash
npm run dev:frontend-admin
```

Opens at [http://localhost:3001](http://localhost:3001) (`/` redirects to `/admin`).
